# Minimal dependencies for Google Colab training (Phase 1 + Phase 2)
# Keep Colab's default NumPy 2.x — FastText falls back to langdetect if needed.
transformers>=4.30.0
datasets>=2.14.0
pandas>=2.0.0
scikit-learn>=1.3.0
pyyaml>=6.0
tqdm>=4.65.0
matplotlib>=3.7.0
seaborn>=0.12.0
scipy>=1.11.0
fasttext-wheel>=0.9.2
langdetect>=1.0.9
